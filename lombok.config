# Stops lombok from looking for child configs
config.stopbubbling = true

# Removes getter prefixes
lombok.accessors.fluent = true

# Chain setters
lombok.accessors.chain = true

# Adds nullable annotations to generated methods when applicable
lombok.addNullAnnotations = javax
